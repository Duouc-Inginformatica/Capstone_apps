PORT=8080

# ============================================================================
# ENVIRONMENT (IMPORTANTE)
# ============================================================================
# Valores: development, production
# En production, JWT_SECRET es OBLIGATORIO y debe tener mínimo 32 caracteres
ENV=development

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=app_user
DB_PASS=change-me
DB_NAME=wayfindcl
DB_SKIP_SCHEMA=0

# Pool de conexiones (opcional)
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=10

# ============================================================================
# SECURITY - JWT
# ============================================================================
# ⚠️ CRÍTICO: En producción debe ser mínimo 32 caracteres aleatorios
# Generar con: openssl rand -base64 32
JWT_SECRET=replace-with-minimum-32-characters-secret-key-here
JWT_TTL=24h

# ============================================================================
# GTFS (Transporte Público) - MEJORADO POR BUSMAPS
# ============================================================================
# Fuente principal: BusMaps mejorado y validado (12,107 paradas, 418 rutas)
# Última actualización: 21 Oct 2025 | Vigencia: 2 Ago - 31 Dic 2025
# Mejoras: +10% cobertura, geometrías corregidas, validado MobilityData
GTFS_FEED_URL=https://s3.transitpdf.com/files/uran/improved-gtfs-transantiago-metrodesantiago.zip

# Auto-sincronización al iniciar servidor (recomendado: false en producción)
GTFS_AUTO_SYNC=false

# Fallback a DTPM oficial si BusMaps falla
GTFS_FALLBACK_URL=https://www.dtpm.cl/descarga.php?file=gtfs/gtfs.zip

# ============================================================================
# GraphHopper Configuration (Motor de Routing)
# ============================================================================
# URL del servidor GraphHopper local
# El backend inicia GraphHopper automáticamente como subproceso
GRAPHHOPPER_URL=http://localhost:8989

# ============================================================================
# DEBUG & LOGGING
# ============================================================================
# Activar logs de debug (solo para desarrollo)
DEBUG=false
DEBUG_SCRAPING=false

# ============================================================================
# APPLICATION INFO
# ============================================================================
APP_VERSION=1.0.0
