# Fiber (HTTP server)
PORT=8080

# MariaDB / MySQL connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=3567
DB_NAME=wayfindcl

# Auth token signing (HS256). Change this in production!
JWT_SECRET=change-me-please
